import 'package:flutter/material.dart';

class FIcons{
  static const editIcon = Icon(Icons.edit, size: 20);
  static const removeIcon = Icon(Icons.delete, size: 20);
  static const settingsIcon = Icon(Icons.settings);
  static const forwardArrowIcon = Icon(Icons.arrow_forward_ios, size: 20);
  static const newspaperIcon = Icon(Icons.newspaper_outlined);
  static const logoutIcon = Icon(Icons.logout_outlined);
  static const addIcon = Icon(Icons.add);
  static const peopleIcon = Icons.people;
  static const accountIcon = Icons.manage_accounts;
  static const bookIcon = Icons.book;
  static const feedbackIcon = Icons.feedback_sharp;
  static const infoIcon = Icon(Icons.info);
  static const termsIcon = Icon(Icons.file_copy);
  static const pPolicyIcon = Icon(Icons.policy);
}